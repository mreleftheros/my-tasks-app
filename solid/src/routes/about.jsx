const About = () => {
  return (
    <section class="section">
      <div class="container flow">
        <p>
          This is a MERN stack project using NodeJs MySQL and Express at the
          backend and SolidJs at the frontend. Also deployed to different
          platforms.
        </p>
      </div>
    </section>
  );
};

export default About;
